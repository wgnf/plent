import 'package:flutter/cupertino.dart';

class LoginDecision extends StatelessWidget {
  const LoginDecision({Key? key}) : super(key: key);

  // TODO: Buttons: With account / Without account
  // TODO: Analytics event First Use
  // TODO: Analytics event which method was used

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Coming Soon (LoginDecision)'),
      ),
    );
  }
}
